directive @authenticated on FIELD_DEFINITION

enum Role {
  ADMIN
  MEMBER
  CONTRIBUTOR
}

type Streak {
  start: String
  active: String
}

type User {
  id: String!
  email: String!
  name: String!
  role: Role!
  photo: String
  about: String
  streak: Streak
  wishlist: [Book]
}
